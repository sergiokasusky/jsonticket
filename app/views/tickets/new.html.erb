<h1>Nuevo reporte</h1>
<%= params.inspect %>
<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <div class="field">
    <%= f.label :desc_corta %><br>
    <%= f.text_field :desc_corta %>
  </div>
  <div class="field">
    <%= f.label :detalles %><br>
    <%= f.text_field :detalles %>
  </div>
  
  
  <%= f.hidden_field :equipo_id, :value => params[:equipo_id] %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Regresar a reportes de este equipo', cliente_equipo_tickets_path %>
